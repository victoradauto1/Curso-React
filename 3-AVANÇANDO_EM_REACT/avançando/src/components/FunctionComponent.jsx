import React from 'react'

const FunctionComponent = ({fun}) => {
  
  
  
  return (
    <div>
        <button onClick={fun}> Troque a Frase</button>
    </div>
  )
}

export default FunctionComponent